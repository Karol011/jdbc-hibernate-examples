package com.sda.hibernate.todo;

import com.sda.hibernate.commons.connection.HibernateUtil;
import org.hibernate.Session;
import org.hibernate.engine.spi.SessionImplementor;
import org.testng.Assert;
import org.testng.annotations.Test;

public class ZadaniaHibernate {

//    @Test(testName = "zad. 1 - Połącz się ze swoim serwerem bazy danych MySQL")
//    public void shouldConnectToDatabase() {
//        Session session = HibernateUtil.getSessionFactory().openSession();
//        boolean isConnected = ((SessionImplementor) session).getJdbcCoordinator().getLogicalConnection().isPhysicallyConnected();
//
//        Assert.assertTrue(isConnected);
//    }


}
